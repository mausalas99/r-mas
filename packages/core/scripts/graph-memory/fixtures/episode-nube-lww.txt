On 2026-08-14 Mauricio confirmed clinicalOps last-write-wins is union-on-join, not whole-document replace. A join or profile push must not drop patient_team_assignment that the sender has not pulled yet. The Cloudflare Worker uses a 30s CPU budget for the sala snapshot. This decision lives in cloud/sync-worker/src/clinical-ops-lww.js. It depends on the Nube sync worker. It superseded earlier whole-doc replace behavior.
