{
  "name": "rplus-sync-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../../scripts/build-cloud-mobile.mjs && node ../../scripts/build-cloud-interno.mjs",
    "dev": "wrangler dev",
    "predeploy": "node ../../scripts/build-cloud-mobile.mjs && node ../../scripts/build-cloud-interno.mjs",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply rplus-sync --local",
    "db:migrate:remote": "wrangler d1 migrations apply rplus-sync --remote",
    "test": "node --test src/**/*.test.js",
    "estimate:free": "node scripts/estimate-free-tier.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
