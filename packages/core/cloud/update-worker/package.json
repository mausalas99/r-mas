{
  "name": "rmas-update-feed-worker",
  "private": true,
  "version": "1.0.0",
  "description": "R+ update feed — probes GitHub then GitLab, rewrites yml asset URLs to absolute. No bindings, no tokens.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
