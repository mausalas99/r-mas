/**
 * Conexión dropdown chrome — cloud-sync façade (WS3-1).
 * Delegates to LAN panel until WS3-2 removes features/lan/.
 */
export {
  closeConnectionDropdown,
  openConnectionDropdown,
} from '../lan/panel.mjs';
