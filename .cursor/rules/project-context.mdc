---
description: Living project map for R+ — read before exploring the codebase
alwaysApply: true
---

# R+ Project Context

**Read this first** for code paths. Use this map instead of broad codebase exploration. Only search or read files when this map lacks the detail you need for the task.

**Strategy & docs hub (product trade-offs):** `docs/core/00-system-index.md` → `docs/core/01-vision-north-star.md`  
**Feature / logic / DB indices:** `docs/features/features-index.md`, `docs/logic/logic-index.md`, `docs/database/database-index.md`  
**Large feature designs:** `docs/superpowers/specs/`, `docs/superpowers/plans/`

Design tokens and UI conventions: `design.md`, `public/tokens.css` (see also `docs/core/06-design-system.md`).

---

## Stack

Electron 41 desktop app (Mac arm64/x64, Windows x64). Renderer: ES modules bundled with esbuild (`public/js/app.bundle.mjs`). Main process: `main.js`. LAN HTTP/WS server: `server.js` on port **3738**. Clinical DB: SQLCipher via `better-sqlite3-multiple-ciphers` + Argon2 (`lib/db/`). Tests: `node --test` (colocated `*.test.mjs` / `*.test.js`).

---

## Entry points

| Layer | File | Role |
|-------|------|------|
| Electron main | `main.js` | Window (`app://rplus` via `lib/renderer-protocol.cjs`), auto-updater, IPC, spawns ward server `:3738` |
| Preload bridge | `preload.js` | `window.electronAPI` IPC surface |
| LAN server | `server.js` | Express routes, doc export, interno mobile, WS hub |
| Renderer boot | `public/js/app.js` → `app.bundle.mjs` | Feature registration via `app-runtimes.mjs` |
| UI shell | `public/index.html` + `public/partials/` | Markup assembled by `scripts/build-ui.mjs` |
| Release | `scripts/release.js` | bump, test, build, publish |

---

## Directory map

```
lib/                  Node-side shared logic (importable from main/server)
  db/                 SQLCipher schema, crypto, clinical access, IPC handlers
  doc-generators/     Native .docx (note, indicaciones, listado)
  entrega/            Modo entrega pendientes (v2 templates + completion)
  interno/            Mobile interno/guardia board, QR, vitals, scope
  drive-import/       Google Drive HC/labs import parsers
 lab-repo/           Intrahospital lab portal scraper (ASP.NET HTML parse, PDF fetch)

lan-squad/            LAN sync host: WS hub, host-store (→ host-store/), conflict resolver,
          auth-router, ticket-store, write-queue, team-code
  persistence/        Host durability: CommitBarrier, JSON shards, lab sidecars, SQL v15 repos

public/js/            Renderer modules (pre-bundle)
  features/           UI feature modules (*.mjs) — primary place for new UI work
  app-state.mjs       Patient/state hydration
  clinical-access-runtime.mjs  Session + privileges (façade → clinical-access-runtime/)
  clinico-access-*.mjs  Scope evaluators (cycle, entrega, guardia, patient, teams, unlock)
  storage.js          Legacy localStorage (non-DB paths; split helpers under storage/)

public/interno/       Mobile web client (guardia/interno board)
scripts/              build-ui, bundle-renderer, release, native rebuild
cloud/                equipos-worker (CF Worker+D1+R2), equipos-pages (static UI)
cloudflare/           setup guide + `setup.mjs` for one-command deploy
docs/
  core/               Agent hub: 00-system-index, 01-vision-north-star, architecture
  features/           features-index.md → feature domains
  logic/              logic-index.md → parsers, sync engines
  database/           database-index.md → SQLCipher schema map
  logs/               profiling / session notes
  superpowers/        Specs and implementation plans (design before big features)
```

---

## Domain index (where to look)

| Topic | Primary locations |
|-------|-------------------|
| Clinical DB / schema | `lib/db/schema.mjs`, `lib/db/db-manager.mjs`, `lib/db/ipc-handlers.mjs`, `lib/db/ipc-handlers.test.mjs` (fake `ipcMain` harness) |
| Roles / privileges | `lib/db/clinical-privileges.mjs`, `lib/db/clinical-access-*.mjs` (teams, guardia, entrega, …), `public/js/clinical-privileges.mjs`, `public/js/clinico-access.mjs` + `clinico-access-scope/` + `clinico-access-*.mjs`; session bootstrap in `clinical-access-runtime/` |
| LAN sync / rooms | `lan-squad/` (`host-store/` persistence submodules, `conflict-resolver.js`, `bundle-merge.js`), `public/js/features/lan/` — façade `orchestrator.mjs` + `conflicts.mjs`, `entity-versions.mjs`, `patient-delete.mjs`, `patient-delete-batch.mjs`, `patient-entries.mjs`, `historia-sync.mjs`, `host-patient-http.mjs`, `live-sync-emit.mjs`, `push`, `room`, `transport`, `panel`, `runtime`; thin `features/lan-sync.mjs` barrel; root `lan-host-registry.mjs`, `lan-ward-host-registry.mjs`, `lan-sync-{state,diagnostics,bundle-push}.mjs`, `lan-delta-client.mjs`; LWW → `docs/superpowers/specs/2026-06-03-lan-conflict-lww-design.md` |
| Clinical teams / guardia | `public/js/features/clinical-teams/` (`teams-roster-{panel,manage,create,directory,submit,shell}`, `teams-roster-lan-*`, `teams-invite`, `teams-guardia-bridge`), barrel `clinical-teams.mjs`, `clinical-rotation.mjs` |
| Modo entrega | `lib/entrega/` (+ `entrega-prep-checklist.mjs`), `clinical-entrega.mjs`, `entrega-prep-panel.mjs`, `entrega-modal-ui.mjs` |
| Interno mobile | `lib/interno/` (`host-store-db.cjs` census from SQLCipher), `public/interno/`, `server.js` interno routes |
| Cloud sync (Nube) | `cloud/sync-worker/`, `public/js/features/cloud-sync/` (`autostart`, `mutate-bridge`, `cloud-ops-events`, `sync-apply` via `features/sync-apply/`), `clinical-ops-sync.mjs`, `cloud-mobile/`; LAN LiveSync HTTP off by default (`R_PLUS_ENABLE_LAN_SYNC`) | all clinical wards |
| Equipos (Lumify/EKG/US) | `lib/equipos/`, `public/equipos/`, `cloud/equipos-worker/`, `equipos-cloud-config.mjs`, `features/equipos-{board,qr-panel}.mjs` | LAN `:3738` **or** Cloudflare Worker queue |
| Estado actual / monitoreo | `public/js/features/estado-actual-*.mjs` |
| Labs / tendencias | `public/js/labs*.mjs`, `labs-default-refs.mjs` (rangos estándar / previos si SOME sin refs), `labs-reslabs-sanitize.mjs` (whitelist paneles en resLabs), `labs-panel-defs.mjs` / `labs-panel-parse.mjs` / `labs-panel-overlay*.mjs` (paneles + overlay LAN), `cultivo-block-core.mjs`, `features/tendencias.mjs`, `lab-panel.mjs`, `features/lan/lab-panel-overlay-sync.mjs`, paste-anywhere `features/paste-smart*.mjs` |
| Lab repo scraper | `lib/lab-repo/` (`portal-client.mjs`, `portal-html.mjs`, fixtures); spec `docs/superpowers/specs/2026-06-27-lab-repo-scraper-design.md` |
| VPO (documentación) | `public/js/features/vpo.mjs`, `vpo-panel.mjs`, `vpo-data.mjs`, `vpo-text.mjs` |
| Document export | `lib/doc-generators/`, `lib/doc-export-http.js`, `server.js` |
| Updates / downgrade | `lib/update-downgrade.mjs`, `stable-versions.json`, `min-version.json`, `main.js`, Ajustes |
| Debt metrics | `scripts/metrics/` (`lint-tier1.mjs`, `check.mjs`, `boot-graph.mjs`, `baseline.json`); `npm run lint:tier1` (changed paths), `npm run metrics:check` |
| Onboarding / tour | `clinical-onboarding*.mjs`, `tour-targets.mjs`, `features/settings-help/` (tours, help, release notes), barrel `settings-help.mjs` |
| UI shell / pase board | `app-shell.mjs` (chrome/toast/modals), `clinical-quick-export.mjs`, `document-export-client.mjs`, `features/pase-board.mjs`, `public/styles/pase-board.css` |

---

## Build & test

```bash
npm run build:ui          # assemble index.html + bundle renderer
npm start                 # electron (prestart rebuilds natives + bundle)
npm run release:publish   # release pipeline (see README § Desarrollo)
```

**Testing policy (agents):** Do **not** run `npm test` during normal dev — it still launches ~348 files in a single `node --test` (~35s+, poor feedback loop). That monolithic full-suite run is **not** the target workflow; we are moving toward **targeted** runs and CI sharding instead.

- **Local / agent:** `npm run test:one -- path/to/changed.test.mjs` (runs under Electron's Node via `scripts/run-with-electron-node.mjs` — same SQLCipher ABI as the app). Do **not** use bare `node --test` for DB/native suites.
- **LAN kernel refactors:** gate with `orchestrator.test.mjs` + `push`/`room`/`transport` + `lan-sync-wiring.test.mjs` (see `docs/core/07-testing-strategy.md`).
- **DB IPC channels:** `lib/db/ipc-handlers.test.mjs` — fake `ipcMain` + `createUnlockedDbManager` teardown pattern.
- **CI / release:** `npm test` in `.github/workflows/ci.yml` and `scripts/release.js` — full suite gate only there.

New renderer code: edit `public/js/**/*.mjs`, then `npm run build:ui` (or `npm start` which bundles). Do not edit `app.bundle.mjs` by hand.

Packaged files list: `npm run release:sync-pack` updates electron-builder `build.files`.

---

## Conventions

- **ESM** in renderer and most `lib/` (`.mjs`); CommonJS in `main.js`, `server.js`, `lan-squad/`.
- **Features pattern**: module exports `windowHandlers`, registered in `app-runtimes.mjs`.
- **IPC**: add channel in `preload.js` + handler in `main.js` or `lib/db/ipc-handlers.mjs`.
- **LAN routes**: add to `lan-squad/host-router.js` or `server.js`; auth via `auth-router.js`.
- **Schema changes**: bump in `lib/db/schema.mjs`, migration + test in `lib/db/schema.test.mjs`.
- **Spanish UI** copy; code/comments mix EN/ES like surrounding files.
- **No scope creep**: match existing patterns; minimal diffs.
- **Conexión / Opciones chrome (HARD):** Fix sharp corners with `border-radius` only. **Never** shrink `#connection-dropdown` / modal width (keep `min(96vw, 960px)`), padding, or Electron window size when rounding corners. Missing `.cloud-sync-inset-group` styles ≠ “make the sheet smaller.”

---

## Changelog (newest first)

Maintained incrementally — see `sync-context-on-commit.mdc`. Max ~20 entries.

- **2026-08-07** `release-8.0.5`: **LAN LiveSync retired** — `lan-squad/` + `features/lan/` removed; Nube-only sync (`cloud-sync/`); ward `server.js` dev-only (`R_PLUS_DEV_WARD_SERVER=1`); Historia clínica removed; Interno MIP + Equipos cloud-only; R+ Móvil dedicated bundle; docs/tours (`RELEASE_NOTES_8.0.5.txt`, `feat-interno-mip-nube.md`, `01-vision-north-star.md`).
- **2026-08-07** `release-8.0.4`: Worker pull snapshot-before-SELECT + mutation prune; clinicalOps fuera del censo; iPad hidrata equipos tras censo; desktop Nube conserva matches estructurales; `pull-strategy.js`, `mutate-bridge.mjs`, `mobile-team-patient-scope.mjs`.
- **2026-08-06** `release-8.0.3`: Eventualidades sin tab Labs (solo bitácora + compose) + reingreso Nube + admin Equipos + clinicalOps por sala + sync auto; `eventualidades-panel-html.mjs`, `eventualidades-render.mjs`, `cloud-census-collect.mjs`, `sync-runtime-cycle.mjs`.
- **2026-08-06** `release-8.0.2`: Diagnóstico Nube (cola/errores/trazas) + sala `torre`→Torre HU + salir equipo resuelve guardias + todos `patientId`; `cloud-sync-diagnostics.mjs`, `panel-cloud-diagnostics.mjs`, `clinical-access-teams-membership.mjs`.
- **2026-08-05** `release-8.0.1`: hotfix pull Nube (apply censo sin LAN wiring) + Worker push atómico con reintento; `orchestrator-boot.mjs`, `patient-entries.mjs`, `cloud/sync-worker/sync.js`.
- **2026-08-05** `release-8.0.0`: Nube todas las salas + R+ Móvil + app://; LAN opt-in; tombstones LWW; `docs/RELEASE_NOTES_8.0.0.txt`, `cloud-mobile/`, `renderer-protocol`.
- **2026-08-05** `release-7.9.8`: All clinical wards on Nube; LAN LiveSync off by default (`R_PLUS_ENABLE_LAN_SYNC=1`); `app://rplus` + `sync-apply` (Nube delete/bulk) + Worker LWW tombstone wipe; `docs/RELEASE_NOTES_7.9.8.txt`, `renderer-protocol`, `host-store-db`, `lww.js`.
- **2026-08-05** `release-7.9.6`: Nube **Sala 1/2/E** rooms distintos + censo por alcance (R4/Admin sala-wide desktop; iPad por equipo) + prune fantasmas + EA monitoreo stub-safe; `sala-allowlist`, `pull-apply`, `patient-scope-prune`, `build:cloud-mobile`.
- **2026-08-05** `cloud-sync`: Nube separa **Sala 1 / Sala 2 / Sala E** como rooms distintos (ya no bucket "Sala"); `sala-allowlist`, admin Salas, `ensure-turn`.
- **2026-08-05** `release-7.9.5`: R+ Móvil Nube (`/mobile/`+QR) + sala mensual `YYYY-MM` + `active_room_id` + census autostart/seed + signos LWW + COAG + LAN restore; `docs/RELEASE_NOTES_7.9.5.txt`, `cloud-mobile/`, `autostart.mjs`, `004-active-room.sql`.
- **2026-08-03** `hybrid-h`: Apple Hybrid UI materials/overlays/chrome onto main; tokens + ui-overlay + Conexión/labs/pase/eventualidades polish.
- **2026-08-03** `ttd-tooltime`: Labs pane nav + EA Tab spine + Conexión light-switch; `doc-queue-nav.mjs`, `estado-actual-panel-registro-tab.mjs`, `panel-session-gate.mjs`.
- **2026-08-03** `eventualidades-labs-timeline`: Labs view is auto timeline (`labsText`) — no Interpretación dock; autosend restored; local color soften on Guardia/Conexión/bulk/pendientes/EV; `lab-eventualidad-autosend.mjs`, `eventualidades-panel-html.mjs`.
- **2026-08-03** `release-7.9.3`: Eventualidades Labs (`labsText`) + Nube LWW/slim/timing + EA registro limpio + consolidación labs misma hora; `eventualidades-labs-ui.mjs`, `cloud-op-slim.mjs`, `lan-patient-merge-eventualidades.mjs`, `docs/RELEASE_NOTES_7.9.3.txt`.
- **2026-08-03** `release-7.9.2`: censo (rescates insulina + dieta suplemento) + Conexión status/Opciones + cultivos/EA; `censo-meds-format.mjs`, `panel-conexion-views.mjs`, `docs/RELEASE_NOTES_7.9.2.txt`.
- **2026-08-03** `release-7.9.1`: recuperación Nube (códigos + Conexión por pasos) + borrado masivo pacientes; `docs/RELEASE_NOTES_7.9.1.txt`.
- **2026-08-03** `patients-bulk-delete`: multi-select sidebar + batch delete (local + LAN/Nube purge); `patients-bulk-*.mjs`, `lan/patient-delete-batch.mjs`.
- **2026-08-02** `cloud-sync`: rebuild ⇄ Conexión chrome on Nube login/logout (session gate); stepped UI token-without-room; `panel-session-gate.mjs`, `panel-conexion-handlers.mjs`.
- **2026-08-02** `nube-recovery-ux`: recovery codes + admin reset; Conexión steps (Connect→Sala→Equipo→Más); logged-out shows only Connect; `cloud/sync-worker` auth-recovery, `public/js/features/cloud-sync/panel-steps-html.mjs`.
- **2026-08-02** `cloud-sync-7.9`: Nube Free pilot for **Sala + Torre HU** only (overrides LAN); Inters/UX/Eme/Área A stay LAN; offline + uncapped labs; `cloud/sync-worker`, `public/js/features/cloud-sync/`.
- **2026-08-01** `release-7.8.1`: diagnósticos/censo en Datos ya no se pierden en merge LAN; `mergeCensoPatientFields` ignora `['']`; `lan-patient-merge.mjs`, `patient-diagnosticos.mjs`.