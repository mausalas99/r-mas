---
description: Living project map for R+ — read before exploring the codebase
alwaysApply: true
---

# R+ Project Context

**Read this first.** Use this map instead of broad codebase exploration. Only search or read files when this map lacks the detail you need for the task.

Design tokens and UI conventions: `design.md`, `public/tokens.css`.

---

## Stack

Electron 41 desktop app (Mac arm64/x64, Windows x64). Renderer: ES modules bundled with esbuild (`public/js/app.bundle.mjs`). Main process: `main.js`. LAN HTTP/WS server: `server.js` on port **3738**. Clinical DB: SQLCipher via `better-sqlite3-multiple-ciphers` + Argon2 (`lib/db/`). Tests: `node --test` (colocated `*.test.mjs` / `*.test.js`).

---

## Entry points

| Layer | File | Role |
|-------|------|------|
| Electron main | `main.js` | Window, auto-updater, IPC, spawns LAN server |
| Preload bridge | `preload.js` | `window.electronAPI` IPC surface |
| LAN server | `server.js` | Express routes, doc export, interno mobile, WS hub |
| Renderer boot | `public/js/app.js` → `app.bundle.mjs` | Feature registration via `app-runtimes.mjs` |
| UI shell | `public/index.html` + `public/partials/` | Markup assembled by `scripts/build-ui.mjs` |
| Release | `scripts/release.js` | bump, test, build, publish |

---

## Directory map

```
lib/                  Node-side shared logic (importable from main/server)
  db/                 SQLCipher schema, crypto, clinical access, IPC handlers
  doc-generators/     Native .docx (note, indicaciones, listado)
  entrega/            Modo entrega pendientes (v2 templates + completion)
  interno/            Mobile interno/guardia board, QR, vitals, scope
  historia-clinica/   HC compile, defaults, migration
  drive-import/       Google Drive HC/labs import parsers
  clinical-safety-rules/  Calculator safety evaluation

lan-squad/            LAN sync host: WS hub, host-store, conflict resolver,
                      auth-router, ticket-store, write-queue, team-code

public/js/            Renderer modules (pre-bundle)
  features/           UI feature modules (*.mjs) — primary place for new UI work
  app-state.mjs       Patient/state hydration
  clinical-access-runtime.mjs  Session + privileges
  storage.js          Legacy localStorage (non-DB paths)

public/interno/       Mobile web client (guardia/interno board)
scripts/              build-ui, bundle-renderer, release, native rebuild
docs/superpowers/     Specs and implementation plans (design before big features)
```

---

## Domain index (where to look)

| Topic | Primary locations |
|-------|-------------------|
| Clinical DB / schema | `lib/db/schema.mjs`, `lib/db/db-manager.mjs`, `lib/db/ipc-handlers.mjs` |
| Roles / privileges | `lib/db/clinical-privileges.mjs`, `public/js/clinical-privileges.mjs` |
| LAN sync / rooms | `lan-squad/` (`conflict-resolver.js`, `bundle-merge.js`), `public/js/features/lan/*` (`orchestrator`, `push`, `room`, `transport`, `panel`, `runtime`), thin `features/lan-sync.mjs` barrel; root `lan-sync-{state,diagnostics,bundle-push}.mjs`; overlap policy → LWW (`docs/superpowers/specs/2026-06-03-lan-conflict-lww-design.md`) |
| Clinical teams / guardia | `public/js/features/clinical-teams/` (`index`, `shared`, `teams-roster*`, `teams-invite`, `teams-guardia-bridge`), barrel `clinical-teams.mjs`, `clinical-rotation.mjs` |
| Modo entrega | `lib/entrega/`, `public/js/features/clinical-entrega.mjs`, `entrega-modal-ui.mjs` |
| Interno mobile | `lib/interno/`, `public/interno/`, `server.js` interno routes |
| Historia clínica | `lib/historia-clinica/`, expediente features |
| Estado actual / monitoreo | `public/js/features/estado-actual-*.mjs` |
| Labs / tendencias | `public/js/labs*.mjs`, `features/tendencias.mjs`, `lab-panel.mjs` |
| Manejo / guías clínicas | `public/js/features/manejo.mjs`, `manejo-guia-*.mjs` |
| Document export | `lib/doc-generators/`, `lib/doc-export-http.js`, `server.js` |
| Updates / downgrade | `lib/update-downgrade.mjs`, `stable-versions.json`, `min-version.json`, `main.js`, Ajustes |
| Debt metrics | `scripts/metrics/baseline.json`, `npm run metrics` (when wired) |
| Onboarding / tour | `clinical-onboarding*.mjs`, `tour-targets.mjs`, `features/settings-help/` (tours, help, release notes), barrel `settings-help.mjs` |
| UI shell / pase board | `app-shell.mjs` (chrome/toast/modals), `clinical-quick-export.mjs`, `document-export-client.mjs`, `features/pase-board.mjs`, `public/styles/pase-board.css` |

---

## Build & test

```bash
npm run build:ui          # assemble index.html + bundle renderer
npm start                 # electron (prestart rebuilds natives + bundle)
npm test                  # full test suite (pretest ensures native db for node)
npm run release:publish   # release pipeline (see README § Desarrollo)
```

New renderer code: edit `public/js/**/*.mjs`, then `npm run build:ui` (or `npm start` which bundles). Do not edit `app.bundle.mjs` by hand.

Packaged files list: `npm run release:sync-pack` updates electron-builder `build.files`.

---

## Conventions

- **ESM** in renderer and most `lib/` (`.mjs`); CommonJS in `main.js`, `server.js`, `lan-squad/`.
- **Features pattern**: module exports `windowHandlers`, registered in `app-runtimes.mjs`.
- **IPC**: add channel in `preload.js` + handler in `main.js` or `lib/db/ipc-handlers.mjs`.
- **LAN routes**: add to `lan-squad/host-router.js` or `server.js`; auth via `auth-router.js`.
- **Schema changes**: bump in `lib/db/schema.mjs`, migration + test in `lib/db/schema.test.mjs`.
- **Spanish UI** copy; code/comments mix EN/ES like surrounding files.
- **No scope creep**: match existing patterns; minimal diffs.

---

## Changelog (newest first)

Maintained incrementally — see `sync-context-on-commit.mdc`. Max ~20 entries.

- **2026-06-05** `win-sqlcipher-pack`: `fetch-sqlite-win.mjs` en `prebuild:win` — prebuild PE win32-x64 para Electron ABI actual; evita *not a valid Win32 application* al empaquetar `.exe` desde macOS.
- **2026-06-05** `6.6.8`: Wi‑Fi header LiveSync + directorio LAN (register→sala→assign); host union clinicalOps, gossip push, admin directory poll; `host-store.js`, `room.mjs`, `clinical-profile-lan-sync.mjs`, `teams-roster-lan.mjs`.
- **2026-06-04** `onboarding-local-first`: sync-mode (LAN vs solo equipo) before DB session; unlock/retry CTAs + Windows session backoff; not gated on ⇄; `clinical-onboarding-main.mjs`, `clinical-panel-host.mjs`, `db-unlock.mjs`.
- **2026-06-04** `mobile-pwa`: Safari A2HS + barebones chrome (sin censo PDF, Mi Perfil, Ajustes, pestaña Salida); `/mobile/` stub, `mobile-web.mjs`, `mobile.css`, `expediente-tabs.mjs`.
- **2026-06-04** `lan-mobile-join`: auth exchange uses request Host; mobile auto-join sala + resume sync; 20s sync-bundle fetch + host-empty toasts; host must Unirse before copy invite; `transport.mjs`, `push.mjs`, `panel.mjs`.
- **2026-06-04** `lan-ward-ux`: rate-limit exempt LAN API; ping 500ms timeout; discovery concurrency 6 (was 32); Mi rotación fail-open ≤3.5s + pause ⇄ scan while open; `room.mjs`, `teams-roster*`, `lan-host-subnet-discovery.mjs`.
- **2026-06-04** `lan-mobile-permanent`: iPad link `/mobile/?token=…` + sharer params (bookmarkable, no ticket TTL); sala invite stays one-time `/join/req_`; `buildPermanentMobileJoinUrl`, `panel.mjs`.
- **2026-06-04** `lan-invite-split`: ⇄ panel splits **iPad/móvil** (copy identity link) vs **otra Mac/sala** (ticket only, own @usuario); `appendMobileSharerParamsToJoinUrl`, `panel.mjs`, `transport.mjs`.
- **2026-06-04** `lan-mobile-sharer`: iPad mirrors invite sharer (`user`/`name`/`rank`/`sala`/`room`) — silent bundle sync, no auto-open ⇄ blocking taps; distinct from desktop «Unirse» UX; `mobile-sharer-sync.mjs`, `transport.mjs`, `panel.mjs`, `app-shell.mjs`.
- **2026-06-04** `lan-host-escalation`: sin R4/admin en red — escalada 10+10+10 min → R3, R2, R1 pueden anfitrionar; timer se reinicia al detectar R4; `lan-host-escalation.mjs`, `lan-host-rank.mjs`, `panel.mjs` scan.
- **2026-06-04** `clinical-profile-gate-6.6.6`: force re-register @usuario vs nombre en guardia on update — bump `CLINICAL_LAN_PROFILE_GATE_VERSION`, `ensureLanProfileGateDeviceReset`, empty prefills + Spanish hints; `clinical-settings.mjs`, `clinical-onboarding.mjs`, registration modal.
- **2026-06-04** `lan-single-host-r1`: R1–R3 cannot be turn host — rank gate before discovery; Electron LAN UI role defaults client; `canLocalMacBeLanHost` / `isClinicalRankConfiguredForLan`; `lan-host-rank-policy.mjs`, `transport.mjs`, `panel.mjs`, `storage.js`.
- **2026-06-04** `lan-single-host-consolidation`: `startedAt` disk meta + `GET /host-rank`; `resolveHostElection` + `lan-host-consolidation.mjs` (push-before-handoff); subnet discovery merge; transport/panel election scan; `docs/superpowers/plans/2026-06-04-lan-single-host-consolidation.md`.
- **2026-06-04** `lan-directorio-sync`: fix users missing while team membership syncs — export teammates by membership, stub→@usuario upgrade, tombstone resurrection, handle-collision merge; `clinicalOpsTrace` in ⇄ diagnostics; `lib/db/clinical-ops-sync.mjs`, `lan-sync-diagnostics.mjs`.
- **2026-06-03** `cold-start-phase2`: esbuild `splitting` + chunks, lazy settings-help/platform (`lazy-feature-routes.mjs`), Chart UMD in HTML + `vendor-loader.mjs` fallback (revised from ESM-only BN-09); metrics excludes generated bundles; pack/chunk smoke tests (BN-09–12).
- **2026-06-03** `cold-start-shell`: trim `app-shell.mjs` (≤800 lines) — doc export → `document-export-client.mjs`, confetti → `chrome.mjs`, patient defaults → `patients.mjs`, quick export → `clinical-quick-export.mjs`; shell re-exports preserved.
- **2026-06-03** `cold-start-clinical-teams`: split `clinical-teams.mjs` → `features/clinical-teams/` (`index`, `shared`, `teams-roster*`, `teams-invite`, `teams-guardia-bridge`); thin barrel `clinical-teams.mjs`.
- **2026-06-03** `cold-start-platform`: split `platform.mjs` → `features/platform/` (`index`, `audit`, `offline`, `import-backup`, `updater`, `runtime`, `shared`); thin barrel `platform.mjs`.
- **2026-06-03** `cold-start-settings-help`: split `settings-help.mjs` → `features/settings-help/` (`index`, `tour-*`, `help-content`, `release-notes`, `settings-dropdown`); thin barrel `settings-help.mjs`.
- **2026-06-03** `cold-start-lan`: consolidate LAN under `features/lan/`; thin `lan-sync.mjs` barrel; `orchestrator.mjs`, `push.mjs`, `room.mjs`, `transport.mjs`, `panel.mjs`, `runtime.mjs`.
- **2026-06-03** `lan-conflict-dead-code`: post-LWW cleanup — `lan-conflict-silent-match.mjs` (boot), removed modal/defer/reopen path from `lan-sync.mjs`; legacy draft clear in panel only.
- **2026-06-03** `lan-conflict-lww`: renderer applies LWW on overlap (no modal/draft); `applyLwwConflictLocally`, `notifyLwwOverwrite`, host cache sync; `lan-sync.mjs`, `lan-sync-push.mjs`.
- **2026-06-03** `release-6.6.2`: LAN ward-ready patch — ops outbox, wiring imports, host HC GET from bundle entries; `docs/RELEASE_NOTES_6.6.2.txt`.
- **2026-06-03** `lan-ward-ready`: ops/bundle decouple, `clinical_ops` outbox drain+abort, QUEUED toasts, host revision on all PUTs; `lan-sync-push.mjs`, `host-router.js`.
- **2026-06-03** `lan-ward-ready-spec`: remediation design; `docs/superpowers/specs/2026-06-03-lan-ward-ready-remediation-design.md`.
- **2026-06-03** `lan-host-clinical-ops-db`: host PUT clinical-ops → SQLCipher when unlocked; bundle `clinicalOps` cache from DB export (IM-13a/b); `lan-squad/host-store.js`.
- **2026-06-03** `lan-sync-improvements`: Phases 0–3 — modular sync (`lan-sync-push/room/transport/panel`), merge registry, SQL outbox v9, host DB `clinicalOps`, pin/diagnostics; `docs/superpowers/specs/2026-06-03-lan-sync-improvements-design.md`.
- **2026-06-03** `release-6.6.1`: LAN sync Phases 0–3 (HTTP-primary push, SQL outbox v9, clinical-ops API, modular lan-sync, host DB clinicalOps); `docs/LAN_SYNC_6.6.1_RISK_ANALYSIS.md`.
- **2026-06-03** `release-6.6.0`: @usuario sin ⇄ obligatorio; merges cloud (directorio LAN, iPad ticket, labs batch-copy); `clinical-profile-lan-sync.mjs`, `lab-history-batch-copy-modal.mjs`.
- **2026-06-03** `release-6.5.9`: LAN directorio, fusión `clinical_users`, entrega/equipos; `pushClinicalOpsLanNow`.
- **2026-06-03** `release-6.5.8`: interno móvil QR, entrega pendientes v2, downgrade a estable; `public/interno/`, `lib/interno/`, `lib/entrega/`, `lib/update-downgrade.mjs`, `stable-versions.json`.
- **2026-06-02** `interno-guardia-mobile`: mobile board, host discovery, QR panel (`interno-qr-panel.mjs`), vitals, interno routes in `server.js`.
- **2026-06-02** `entrega-pendientes-v2`: schema v8 templates; `lib/entrega/`, `entrega-modal-ui.mjs`, `clinical-entrega.mjs`.
- **2026-06-02** `stable-downgrade`: catálogo curado + UI Ajustes; `stable-versions-catalog.js`, `min-version.json`.
- **2026-06-01** `lan-teams-decoupled`: persistent clinical teams decoupled from daily guardia; `clinical-teams.mjs`, `effective-team-code.js`.
- **2026-06-01** `guardia-lan-hub`: guardia connection panel, host discovery, mobile join link, census scope by role.
